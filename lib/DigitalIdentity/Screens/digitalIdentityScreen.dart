import 'package:flutter/material.dart';

class DigIDScreen extends StatefulWidget {
  DigIDScreen({Key? key}) : super(key: key);

  @override
  State<DigIDScreen> createState() => _DigIDScreenState();
}

class _DigIDScreenState extends State<DigIDScreen> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
